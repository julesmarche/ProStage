{% extends 'base.html.twig' %}


{% block  contenu %} 
    <table class="table table-hover">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ formation.id }}</td>
            </tr>
            <tr>
                <th>NomLong</th>
                <td>{{ formation.nomLong }}</td>
            </tr>
            <tr>
                <th>NomCourt</th>
                <td>{{ formation.nomCourt }}</td>
            </tr>
        </tbody>
    </table>

    <a class="btn btn-outline-primary" href="{{ path('formation_index') }}">Retourner à la liste</a>

    <a class="btn btn-outline-primary" href="{{ path('formation_edit', {'id': formation.id}) }}">Modifier</a>

    {{ include('formation/_delete_form.html.twig') }}
{% endblock %}
